# HANDOVER

給下一個接手的人（或下一段對話）。日期：2026-08-23。

**這份是入口。** 讀完再依需要進 `docs/`，不要從 `docs/PLAN.md` 開始 ——
那份有幾章已經被實作結果推翻，改寫過但仍以規劃視角書寫。

---

## 一句話

**已經在真機上跑起來了**：查英文、發音、注音打中文查漢英，都在板子上動過。

第一次上機只踩到兩個問題，而且都不在硬體那一層：SD 卡的路徑（`/DICT/`）
與**字模快取沒做**（每張畫面讀 SD 兩千多次，慢到不能用）。三段硬體膠水
（ILI9341 初始化、矩陣掃描、SD 開檔）一次就對 —— 那是 PLAN.md §4
「後台與前端分離」的直接結果。

---

## 現在有什麼

| | 狀態 |
|---|---|
| 字典檔格式 | 定稿，`docs/FORMAT.md` |
| PC 端轉檔工具 | 完成，`tools/`，47 項自我驗證 |
| 共振峰合成器（中英共用） | 完成，Python + C 兩份，比對通過 |
| 查詢後台 | C 實作完成，與 Python 逐筆比對通過 |
| 16×16 2bit 字模 | 完成，`tools/mkfont.py` |
| UI 排版 | C 實作完成，與 Python 預覽**逐像素**相同（含捲動） |
| 4bpp 畫布 | 完成，`firmware/fbuf.c`，37.5 KB |
| 鍵盤解碼 | 完成，`firmware/keys.c`，用實測真值表 |
| 前景狀態機 | 完成，`firmware/app.c`，兩個畫面 |
| 板子端 sketch | **已上機**：畫面、鍵盤、SD 都會動 |
| 偏移版編譯 | 完成，`build_offset.bat`，佈局檢查通過 |
| 選單封面圖 | 完成，`assets/RetroDict.ino.RAW`，`tools/mkicon.py` 可重產 |
| 發音 | 完成並實機驗過：Fn+1，有音標唸音標、沒有就用字母規則現場推 |
| CE 繁體化 | 完成：索引鍵與詞頭是繁體，簡體在 `SIMP` 欄 |
| 注音 IME | 完成並實機驗過：Fn+2 切換，1360 筆碼表逐筆比對 |

純 C 那幾支加起來 **13.9 KB 程式碼、69.5 KB 唯讀表、靜態 RAM 0**
（`-Os`、Cortex-M0+，量自 build 產物的 `.o`）：

| 模組 | 程式碼 | 表（rodata） |
|---|---:|---:|
| `app.c` | 3,824 | 259 |
| `synth.c` | 3,234 | 1,848 |
| `ui.c` | 1,474 | 73 |
| `dict.c` | 1,434 | 8 |
| `font.c` | 1,420 | 9 |
| `lts.c` | 716 | 3,003 |
| `keys.c` | 698 | 256 |
| `ime.c` | 558 | 65,112 |
| `fbuf.c` | 464 | 0 |
| `speech.c` | 422 | 635 |
| **合計** | **14,244** | **71,203** |

表比程式碼大 5 倍，而且幾乎全在 `ime.c` 的注音碼表（65 KB）—— 那是資料不是邏輯，
放 flash 不佔 RAM。

整支 sketch 編出來是 flash 209,420 B（10%）、靜態 RAM 175,220 B（66%）。
RAM 的組成（`arm-none-eabi-nm --size-sort`）：

| | |
|---|---:|
| 字模碼位表快取 | 40,960 |
| 4bpp 畫布 | 38,468 |
| 發音 PCM 緩衝 `g_pcm` | 24,000 |
| 合成器工作區 `g_syn_work` | 16,000 |
| 前景狀態 `g_app` | 10,620 |
| 音訊 DMA（mixer + buffers） | 16,384 |
| 合成器音段／8bit 暫存 | 12,000 |
| 其餘（SD 函式庫、USB、core1 堆疊…） | ~16,800 |

那 40 KB 的字模快取 **不是可有可無的最佳化**：沒有它，每畫一張畫面要讀 SD 兩千多次，
實機上按一個字母要等好幾秒（見 firmware/README「效能」）。

---

## 怎麼從零重建

repo 不含字典資料與字型（體積大、授權要各自遵守）。三步：

### 1. 下載原始資料到 `data/`

```bash
curl -L -o data/ecdict.csv https://raw.githubusercontent.com/skywind3000/ECDICT/master/ecdict.csv
curl -L -o data/cedict.txt.gz https://www.mdbg.net/chinese/export/cedict/cedict_1_0_ts_utf-8_mdbg.txt.gz
gunzip data/cedict.txt.gz
curl -L -o data/NotoSansSC-VF.otf https://github.com/notofonts/noto-cjk/raw/main/Sans/Variable/OTF/Subset/NotoSansSC-VF.otf
```

`ecdict.csv` 66MB（MIT）、`cedict.txt` 10MB（CC-BY-SA）、
`NotoSansSC-VF.otf` 15MB（OFL 1.1）。
字型另外用到 Windows 內建的 `NotoSansTC-VF.ttf` 與 `NotoSans-Regular.ttf`
（同為 OFL）—— 換平台要自己補這兩份，路徑寫在 `tools/mkfont.py` 的 `FONTS`。

### 2. 產生 SD 卡內容

```bash
python tools/mkdict.py ec data/ecdict.csv out/DICT
python tools/mkdict.py ce data/cedict.txt out/DICT
python tools/mkfont.py out/DICT
python tools/mkdict.py check out/DICT
```

產物約 113MB：`EC.IDX/DAT`、`CE.IDX/DAT`、`ECC.IDX`（常用詞索引）、
`FONT.BIN`。整個 `out/DICT` 複製到 SD 卡根目錄。

### 3. 驗證

```bash
python tools/tests/test_roundtrip.py
python firmware/compare.py 3000
python firmware/compare_synth.py
python tools/ui_preview.py
python tools/say.py --experiment
python firmware/compare_ui.py
firmware/test_app.exe out/DICT keys
python tools/ui_session.py
```

C 的部分要先用 `firmware/build_pc.bat`、`build_synth.bat`、`build_ui.bat` 與
`build_app.bat` 編譯（需要 Visual Studio 2022 Community）。

板子的 uf2（**要燒的是偏移版**）：

```bash
build_offset.bat [arduino-cli 的路徑] [rp2040-retro-loader 的路徑]
```

需要 arduino-cli 與 `rp2040:rp2040` 5.6.1。SD 卡要有 `/DICT/`
（`EC.IDX`、`EC.DAT`、`ECC.IDX`、`FONT.BIN`）。

實測佈局（2026-08-25 重編）：image `0x10004000..0x10039100`（217,344 bytes），
上限 `0x101ff000`，餘裕 1,859,328 bytes。向量表 `SP=0x20042000 Reset=0x100040e3`，
載入器 `app_present()` 的條件都通過。

⚠️ 上面的 217,344 是**燒進 flash 的 image 大小**，不是 uf2 檔案大小。UF2 一個
block 是 512 bytes 但只裝 256 bytes 酬載，所以檔案約是 image 的兩倍：
`RetroDict.ino.uf2` 849 blocks = 434,688 bytes，合併跳板後的
`RetroDict_standalone.uf2` 913 blocks（跳板 12 + 本體 849 + 填充 52）
= 467,456 bytes。引用時別把 image 大小當成檔案大小。封面圖
`assets/RetroDict.ino.RAW` 也會被複製到 `build_offset/`，與 uf2 同放 SD 根目錄。

---

## 已定案，不要再翻案

| 項目 | 決定 |
|---|---|
| 中文輸入 | **注音**，搬 `pico_keyboard_ime_terminal` 的引擎 |
| 最終產物 | **偏移版**（連結 `0x10004000`），見 PLAN.md §2.8 |
| 授權 | GPL-3.0 |
| 字典資料 | ECDICT + CC-CEDICT，不碰商業字典 |
| 繁簡 | **不做轉換**。CE 用來源自帶的繁體（索引鍵與詞頭是繁體、簡體降級成 `SIMP` 欄）；EC 的釋義維持簡體，理由見下 |
| 鍵盤對照表 | 用 KeyboardTester README 的**實測真值表** |
| 架構 | 後台與前端嚴格分離，後台可在 PC 上測 |
| 字典檔格式 | v1 定稿，見 FORMAT.md |
| 前綴候選 | 常用詞優先（模式 B）寫死，設定選單延後 |
| 發音 | **共振峰合成，中英共用一套**。不錄音、不用 SAM/eSpeak |
| 字型 | **16×16 2bit 灰階**，Noto 轉出的 `FONT.BIN` |
| 激發源 | **每個音素／音節各自重置**，不做整詞連續。與整詞響度正規化互斥，見下 |

### 打字畫面的 Fn+1 唸什麼（2026-08-25）

原本唸的是**候選清單反白的那一筆**，而候選是常用詞優先排的（FORMAT.md §8），
於是打 `cat` 唸出 `catch`、打 `b` 唸出 `back`、打 `cat` 聽起來像 cash
（catch 的 /tʃ/）。使用者一路回報成「尾音有爆音」，我追了兩輪音訊路徑都
找不到 —— **因為音訊完全沒問題**。是使用者自己發現「原來唸的是候選第一個」。

現在的規則：

| 光棒位置 | 唸什麼 |
|---|---|
| 第一列 | **搜尋列裡的字串**。字典查得到就用它的音標，查不到才退到 `lts.c` 的字母規則 |
| 離開第一列 | 使用者反白的那個候選（用它自己的音標） |

配套改了候選排序：**打進去的字如果字典裡有，就讓它當第一列**。分兩種情況，
少做一種就會漏掉常見案例：

1. 完全相符的那筆有在候選裡（`cat`）—— 搬到第一位
2. 它連候選都沒進來（`b`、`co`：前八名全被更常用的詞佔滿）—— 用
   `dict_prefix(..., common_first=0)` 再查一次撈進來，擠掉最後一列。
   純字母序時完全相符的一定排最前（cat < catch < category），所以取一筆就夠

這樣「畫面上反白的」與「Fn+1 唸的」永遠一致，不會再出現「打完整的字卻聽到
別的字」。排序策略本身沒動 —— 常用詞優先對「打一半」仍然是對的。

**這一段沒有自動測試。** `test_app` 的 run 模式是腳本驅動、不做斷言，加斷言
要另外開一個模式。驗的時候用：

    test_app out/DICT run "cat[F1]" x      -> 應該唸 cat，不是 catch
    test_app out/DICT run "cat[DOWN][F1]" x -> 應該唸 catch
    test_app out/DICT run "b[F1]" x         -> 應該唸 b，不是 back

### audio_is_source_active() 不是「播完了」（2026-08-25）

實機回報「母音結尾的字，尾巴多一個 /t/ 的音」（b/bo/boo、c/co/coo、see、go；
`cat` 變成 `cash`；`best` 沒事）。**同一份波形在 PC 上聽是乾淨的** —— 那一句
把範圍直接切到播放端，合成器完全洗清。

根因是 `audio.c` 那個旗標的語意：`active = false` 是在「取樣點都**混進**
緩衝區」時設的，不是「喇叭響完了」。混音器最多領先 DMA 一整個
`AUDIO_BUFFER_SIZE` = 4096 樣本 = **256ms**。

短字（0.20 秒）整個塞得進一個緩衝區，於是：

1. 一次 `audio_mixer_step()` 混完整個字，`active = false`
2. 主迴圈立刻判定「播完了」-> `g_app.speaking = 0` -> `refresh_status()` 重畫
3. 但 DMA 這時才要開始播 —— **重畫是在聲音還在響的時候做的**
4. 重畫拖住主迴圈，這個緩衝區播完時另一個還沒被填成靜音，DMA 就播出
   上一次留在裡面的舊資料。那段殘響就是聽起來像 /t/ 的東西

母音結尾特別明顯，因為那時波形振幅還很大；`best` 這種子音結尾的收得很輕，
所以聽不出來。

**掩護這個 bug 的是一句過期註解**：主迴圈寫著「緩衝區只有 1024 個樣本
（16kHz 下 64ms）」，實際上 `AUDIO_BUFFER_SIZE` 早就改成 4096（256ms）。
64ms 的領先量無關痛癢，256ms 就完全是另一回事 —— 註解讓人不會去想這件事。

修法：`speak_play()` 記下預計播完的時刻（取樣點數 / 取樣率 + 一個緩衝區的
餘裕），主迴圈要同時滿足「混完了」與「時間到了」才收掉狀態列。另外
`blit_rows()` 開頭也補餵一次混音器。

### 發音快取只做「跟上次一樣」這一種（2026-08-24）

按 Fn+1 唸同一個字第二次時不重新合成。實作只有一個 96 bytes 的鍵緩衝
（`SPEAK_KEY_MAX`）—— **波形本來就還在 `g_pcm` 裡**，播放是讓混音器直接讀
那塊記憶體，唸完也沒人清掉。所謂快取要做的只是記得它是誰的。

鍵是 `[來源標記][原始位元組]`：有音標走 id 串、沒音標走 `lts.c` 的用那個
字串。超過 96 bytes 的（極少數長詞）就不快取直接重算。

**`tone()` 會借用 `g_pcm` 當方波緩衝，所以它必須讓快取失效** —— 這是唯一
一個不明顯的地方，漏掉的話按過測試音（Fn+9）之後再唸同一個字會播出嗶聲。
`tone()` 也會在「合成一個取樣點都沒產出」時響，那條路同樣要失效。

**實機驗過三條路**：重複唸同一個字會命中；Fn+9 之後再唸同一個字不會播成
嗶聲；查不到音標、走 `lts.c` 字串鍵的那條路也會命中（那是不同的分支，
鍵是字串不是 id 串，值得單獨測）。

**沒有做更大的快取**，理由：
- RAM 快取放不下幾個詞：一個詞 0.74 秒 = 11,840 bytes，堆積只剩 ~60KB
- SD 預先算好整個 `ECC.IDX`（2 萬個常用詞）要 237MB，字典本身才 113MB。
  而且共振峰合成當初就是為了避開這個成本才選的（U1/U6/U7 是被「不需要
  錄音」一起關掉的），預算 PCM 等於把那個成本請回來
- 任何韻律或合成參數的改動都會讓預算的快取失效，而那些參數這幾天改了五次

**如果哪天覺得合成太慢，先量再說，而且快取不是第一順位。**
U3-REPORT §1 的「195 倍餘裕」只算了三個共振器（60–80 cycles/取樣點），
沒算 `syn_normalize()`：那裡每個取樣點呼叫一次 `tanh()`、還有數萬次 `double`
乘法，而 Cortex-M0+ 沒有 FPU。以 `dictionary`（11,840 點）為例是 11,840 次
`tanh` + 至少 35,520 次 double 乘法 + 1,480 次 `res_coef`（2×exp + 1×cos）。
真的慢的話，把這些定點化／查表化比做快取便宜得多，也不吃儲存空間。

### 發音音量：增益加在播放層，不是合成器（2026-08-24）

實機反映「音量不夠大」。量過之後發現不是 bug，是**合成器刻意留的餘裕沒被用掉**：
`voice.py` 的 `TARGET_RMS` 0.20、`SOFT_LIMIT` 0.55，波形峰值只用掉 PWM 擺幅的
一半。實測 200 個常用英文詞 + 12 句中文，峰值中位數 62、**最大 70**（滿刻度
±126），中英文一致 —— 軟限幅把峰值壓得很集中，所以餘裕是可預測的。

解法是 `RetroDict.ino` 的 `SPEAK_VOLUME = 435`（1.7 倍），最大峰值變成 119/126，
留 6% 邊際。2.0 倍會削波（140/126）。

**為什麼不調 `voice.py` 的 `TARGET_RMS`**：那兩個常數是 U3 聽判期間用耳朵調
出來的，決定的是「子音相對母音多大聲」這種**內部平衡**；而這裡要的是整體
播放音量，是播放層的事。動合成器的配方會讓 `compare_synth.py` 的參考基準
一起漂移，而且會把當初調好的平衡也一起改掉。

混音器出口有 0..255 箝位（`audio.c`），削波只會削平不會繞回。

**實機確認音量足夠。** 注意剩下的數位餘裕只有 6% —— 如果哪天又覺得不夠大，
不要再往上加這個數字（會削波），要看的是 class-D 模組的增益或 RC 那一級。

### 多讀音一律取第一個 = 一律唸美式（2026-08-25）

ECDICT 的音標欄位有一部分帶兩個讀音，我們取第一個。**那不是隨便取的一個，
是有意義的**：ECDICT 把美式放前面，所以這等於「一律唸美式」。

而且這些不是冷僻字，是日常字：

| 字 | 詞頻 | 音標 | 我們唸 |
|---|---:|---|---|
| `minute` | 308 | `'minit. mai'nju:t` | `'minit`（分鐘） |
| `lead` | 318 | `'li:d. led` | `'li:d`（帶領） |
| `chance` | 613 | `'tʃæns. tʃɑ:ns` | 美式 |
| `plant` | 623 | `'plænt. plɑ:nt` | 美式 |
| `dance` | 1671 | `'dæns. dɑ:ns` | 美式 |
| `desert` | 2196 | `'dezәt. di'sә:t` | `'dezәt`（沙漠） |

兩個讀音的來源有兩類，處理起來的意義不同：

- **英美差異**（plant、dance、cast）—— 取美式是個乾淨的選擇，沒有對錯
- **詞性差異**（desert 沙漠/拋棄、minute 分鐘/微小、conduct 行為/實施）——
  **這一類我們一定會唸錯一半**。查「拋棄」也會唸成 `'dezәt`

**詞性那一類做不到，不是還沒做。** 要按詞性選讀音，得知道「哪個讀音對應
哪個詞性」，而 ECDICT 的 `phonetic` 與 `pos` 是兩個獨立欄位，資料裡**沒有
這層對應關係**。除非換資料來源（見上面那節的代價），否則無解。

真要改善的話，成本最低的是**顯示層**而不是發音層：畫面上把第二個讀音也
列出來，讓使用者自己看到「這個字有兩種唸法」。那不需要新資料，只要
`build_ec.py` 別把第二段丟掉。

### 為什麼英漢的釋義是簡體（2026-08-24 查證）

會反覆有人問，所以把數字留在這裡，免得下次又重查一遍。

**一、來源就沒有繁體。** 掃過 `ecdict.csv` 全部 770,611 筆的 `translation`
與 `definition`：

| 字對 | 簡體筆數 | 繁體筆數 |
|---|---:|---:|
| 电／電 | 31,564 | 0 |
| 国／國 | 19,041 | 1 |
| 体／體 | 17,339 | 0 |
| 学／學 | 12,555 | 0 |

那幾個 1 是雜訊。中文釋義是 100% 簡體 —— **不是轉檔丟掉的**。

**二、CE 做得到是因為來源結構不同，不是我們處理方式不同。**
CC-CEDICT 每條同時給繁體詞頭與簡體詞頭，所以 CE 是**選欄位**；ECDICT 只有
一個中文欄位。對 EC 而言選項只有「轉換」或「不轉」，上面那條「不做轉換」
其實沒有涵蓋到 EC，它是預設落在不轉那邊。

**三、要轉的話成本比想像中低。** 轉換在 PC 端轉檔期做，**韌體零成本**
（跟三聲變調同一個道理）。字型也幾乎不變：EC 釋義用到 6,054 個漢字，其中
5,925 個（97.9%）已經在 CE 的字集裡 —— CE 同時存繁簡詞頭，`FONT.BIN` 早就
兩套都收了。EC 獨有的只有 129 個，約 8KB。

**四、真正的代價是正確性，而且沒得選模式。** opencc 實測，光是「里」在
人名／地名條目裡就錯 694 筆：

    alberic    阿尔伯里克  ->  阿爾伯裡克   (音譯名的里被當成裡面的裡)
    amesbury   埃姆斯伯里  ->  埃姆斯伯裡

`s2t`／`s2tw`／`s2twp` **三種模式全錯** —— 不是選錯模式，是 opencc 沒有
「這是音譯名」的資訊。另外 `[复]` 會變成 `[復]`（應該是 `[複]`），方括號裡
的縮寫沒有上下文可判。而這是 770k 筆、不會有人逐條校對的資料。

**五、結論：不轉。** 不是做不到，是投報比 —— 使用者讀簡體釋義沒有障礙，
換來的是幾千筆散在字典裡、無人校對的錯字，那比簡體字更傷。

真要做的話，**唯一有效的緩解**是把帶 `[人名]`／`[地名]` 標記的條目整條
跳過不轉（27,435 筆，錯誤最集中的地方），其餘用 `s2tw`。這擋得掉最大宗的
一類，擋不掉 `[复]` 那種。

**六、那換一套繁體的英漢資料呢？** 也量過了，三條路都不划算：

- **反轉 CC-CEDICT 當英漢用**：最常用 5,000 詞涵蓋 77.4%、柯林斯三星以上
  78.1%，但**全部有音標的 218,065 詞只涵蓋 8.4%**。而且反查出來是詞對詞，
  沒有詞性與義項，繁體換來的是內容變薄。
- **拿 CC-CEDICT 的繁簡詞對當轉換表**（76,146 對，人工維護，比 opencc 的
  通用表精準）：只覆蓋得了常用詞釋義漢字的 **60.2%**，剩下四成還是得回去
  用 opencc，錯誤又回來了。是部分改善，不是解法。
- **換掉 ECDICT**：**第一個問題不是釋義好不好，是有沒有 IPA。** 那 218,065
  筆音標是整個發音功能的地基（U1/U6/U7 三個風險是被「字典自帶發音資訊」
  一起關掉的），沒有 IPA 就等於把發音退回 `lts.c` 的字母規則推測。目前已知
  同時有「繁體 + IPA + 可用授權」的只有英文 Wiktionary（CC-BY-SA），但它是
  條目化的維基文本不是整齊的 CSV，移植等於重寫整條 `build_ec.py`。
  **注意：外部資料集的可用性與授權沒有查證過，只是知道有這個方向。**

**成本最低的折衷（如果哪天真的要做）**：ECDICT 全部欄位不動，只在 CC-CEDICT
有對應的常用詞上**額外掛一個繁體欄位**（77%），顯示時優先用繁體、沒有就用
原本的簡體。音標／詞頻／詞性／義項一個都不賠，也不做機械轉換所以不會生出
錯字，字型不用多收。代價是 SD 卡多幾 MB 與一點顯示邏輯。

### 為什麼激發源不能連續（2026-08-24，實機抓到）

Python 的 `english.synth()` 對整個詞只呼叫一次 `_voiced_source()`，激發是
連續的。照搬到 C 之後**爆破音整個不見了** —— 板子上的症狀是「apple 的 p
沒聲音」。

原因是 `syn_normalize()` 一次只看一個音素，而塞音的 `pre_len == n`、整段
共用一個增益。前一個母音的共振器殘響被帶進成阻段：apple 的 /p/ 原始 rms
從 47 變成 456（爆破本身沒變，58 -> 55），增益掉十倍，爆破音被自己的殘響
壓死。

要讓連續激發源成立，得**整個詞一起正規化** —— 那正是 RAM 放不下的那件事。
所以這兩者是互斥的，不是「還沒做完」。`KIND_GAIN` 也因此不是暫代品。

共振峰平滑（`smooth_in()`）不受影響，留著。

### 四個已經關閉的風險

- **U1**（Ekho 音檔授權）—— 不需要任何錄音，問題消失
- **U6**（英文合成器選型）—— 中英共用一套
- **U7**（flash 空間）—— 合成器參數表 < 2KB
- **U4**（16×16 字型來源）—— Noto，OFL 1.1

原本最大的三個未知數是被**同一個發現**一起關掉的：字典本身就附了發音資訊
（中文附拼音、英文附音標），所以不需要 g2p 引擎；而 1980 年代的電子字典
本來就是共振峰合成，不是拼接錄音 —— 當年沒有記憶體放 1300 段錄音。

---

## 還沒做的事（按建議順序）

1. ~~**U3 的三組 A/B 聽判**~~ —— **2026-08-24 做完了**。三組**全部聽得出差別**，
   一條規則都沒刪掉：`04`（音節間 60ms 間隙）、`05`（三聲變調）、
   `07`（輕聲看前字）勝出。原本的期待是「刪掉幾條白花的複雜度」，結果相反 ——
   三條都是真的有用，所以變成**要把它們補進 C**（見下一項）。
2. **D4**：生字本／考綱篩選要不要進第一版（ECDICT 已附 `tag` 欄位）
3. **整詞韻律** —— 中文那半做完了，英文那半還沒。
   - ✅ 中文：`syn_syllable_ctx()` + `speech_ids()` 有了音節間隙、輕聲看前字、
     句末拉長，`compare_synth.py` 加了「整串」一節逐項比對。
   - ✅ 英文句末降調：`syn_en_ctx` + `syn_phoneme_ctx()`。Python 是整個詞
     攤成段清單再算，板子上沒那個 RAM，所以改成等價的串流形式（先前掃數
     母音，之後逐格往下降，子音沿用前一個母音的高度）。母音的基頻與 Python
     逐一相同，`compare_synth.py` 直接比 f0 數列，誤差 < 0.5Hz（Q8 進位）。
   - ✅ 英文跨音素平滑：**2026-08-24 聽判通過**。實際做了兩件事，
     綁在同一個開關（`syn_en_ctx.smooth_ms`）：
     1. 共振峰在每個音素開頭 18ms 從前一個音素滑進來（`smooth_in()`）
     2. ~~整個詞共用一條激發源~~ —— **試過，撤掉了**。詳見下面「已定案」。
     **這一項不能跟 Python 逐點比對**（Python 是置中的移動平均，串流只能
     單邊滑入），也找不到堪用的客觀指標 —— 波形的逐點差被振幅主導，量的
     不是喀噠聲。`test_synth.exe ... enw0` 可以關掉平滑重產 A/B。

     **噪音類子音兩個方向都不參與平滑**（2026-08-25 實機回報「非字首的 æ
     變成哨聲」才發現）。塞音／擦音／塞擦音的 `fr.f` 不是聲道共振峰，是
     噪音頻帶的中心（`/s/` 是 5000Hz）—— 沒有發音器官的連續性可言。
     - 滑**入**母音：等於叫一個窄頻寬、有聲激發的共振器從 5kHz 掃到 700Hz，
       就是一聲下滑的哨音。實測 sad 的 æ 起頭 16ms 內 2.5kHz 以上能量
       從 0.1% 衝到 **75.9%**；字首的 æ 沒事，因為沒有前一個音素可滑。
     - 滑**出**到擦音：反過來讓 `/s/` 的噪音帶從母音共振峰起頭，嘶聲變成
       低頻隆隆聲。實測 mass 的 `/s/` 起頭 1.5kHz 以下能量 3.2% -> **63.1%**。

     所以規則是：**平滑只發生在兩邊都是真共振峰的時候**（母音、鼻音、
     邊音、近音、滑音之間）。**實機驗過**：哨聲消失，而 mass／yellow 這類
     有聲對有聲的平滑仍在（要一起驗，否則分不出「修好了」與「整個功能被
     順手關掉了」）。
   - ❌ 整詞響度正規化：`speech.c` 的 `KIND_GAIN` 仍是替代品。這一項是
     真的做不到 —— 要整個詞的波形都在手上才能一起正規化，板子上只有
     8-bit 的 `g_pcm`（24KB），int32 的工作區放不下整個詞。
4. ~~**發音長度上限**~~ —— **2026-08-24 做完**。`SPEAK_MAX_PCM` 24,000 -> 40,000
   （1.5 秒 -> 2.5 秒），RAM 71% -> 77%。
   數字是量出來的：ECDICT 全部 218,062 筆有音標的詞條逐筆算發音長度，
   1.5 秒截掉 5,521 筆（2.53%）、2.0 秒 646 筆、**2.5 秒 75 筆（0.034%）**、
   3.0 秒 13 筆。2.5 秒是膝點，再往上加 8KB 只多救 62 筆。
   注意 `__StackTop` 在 SCRATCH_Y，core0 的堆疊是固定 2KB，**不吃這 16KB**
   —— 被吃掉的是堆積（剩 59,836 bytes，原本 75,836）。
   **實機驗過**：開得了機、查得到字，所以 60KB 堆積對 SD 函式庫是夠的。
   這件事 PC 上驗不出來，再往上加 RAM 的話要重驗一次。
5. **簡體也能查**（可選）—— CE 現在只認繁體鍵。做法是索引加別名指向同一個
   `.DAT` 位置（只多 32 bytes/筆），要動 `container.build()`

---

## 這個專案最貴的教訓

寫在這裡是因為它反覆出現，而且每次都花掉最多時間。

### 一、「測試通過」要先確認測試真的測到東西

| 案例 | 症狀 |
|---|---|
| C/Python 比對 | 中文鍵是 UTF-8，走命令列會被字碼頁弄壞所以被跳過 —— 腳本印「完全一致」，但**半個字典從沒驗證過** |
| U3 聽判第一輪 | 測試檔 `01` 全是 `ma`，看似乾淨的對照組，讓「擦音比母音大 1700 倍」整類 bug 隱形 |
| 英文母音 | 測試檔把 `father` 與 `but` 寫進同一個字串，黏成一句念出來 |
| 逐音節比對 | `compare_synth.py` 只比單音節／單音素，於是「單音節緩衝只有 4000 點、三聲 300ms 會被截掉」這個 bug 在板子上活了很久 —— 因為**沒有任何一項測試餵超過一個音節** |
| 爆破音回歸 | 第一版的塞音測試拿「關掉平滑」當基準，可是兩種模式都帶著同一個 bug，比出來 1.00 倍、什麼都沒驗到。**參考點跟被測物一起壞掉的測試等於沒有測試** —— 改成拿同一個詞裡詞首的同一個音素當基準才驗得出來 |

### 二、壞的常常是尺，不是被量的東西

| 案例 | 假象 |
|---|---|
| 自相關沒正規化 | 把下降的四聲量成「上升到 276Hz」 |
| 粗糙 DFT 每 4 點取樣 | 3kHz 以上全是混疊，量出 `a` 和 `i` 的頻譜一模一樣 |
| 波形相關係數 | 擦音兩邊亂數不同本來就不會相關，`er` 的 F2/F3 靠太近會拍頻 —— 25 項全紅其實是指標選錯 |

現在 `tools/synth/spectrum.py` 用真正的 radix-2 FFT，不抽樣不取巧。

### 三、真實資料一定比規格髒

ECDICT 與 CC-CEDICT 挖出來的，全部寫成回歸測試：

- **同一個音有多種字元**：schwa 三種（西里爾 U+04D9 比真 IPA 還常見）、
  `ɛ` 三種（IPA／西里爾／希臘）、主重音三種
- **mojibake**：`^` 其實是 `g`、連續反斜線其實是 schwa
- **看起來像分隔符的其實不是**：逗號是**次重音記號**，拿去 split 會靜默
  丟掉 15% 的發音。**句點也是**（`.vɒlә'tiliti`、`'skrʌb.wumәn`）——
  含句點的 7,181 筆裡，真正當分隔符的只有 169 筆，那 169 筆的特徵是
  「句點後面接空白」（castle 是 `'kæsl. 'kɑ:sl`，美式在前英式在後）。
  沒處理的話 castle 會被唸成 kæsl-kɑ:sl 兩次。分號則一直都是分隔符。
  **逗號也一樣**：`, `（逗號加空白）是分隔符（bund 是 `'bʌnd, bʌnt`，2,623 筆），
  貼著音素的逗號才是次重音。**但要從索引 1 開始找** —— 有 29 筆長成
  `", kælis'θenik"`，開頭的次重音逗號後面多打了一個空格，從 0 找會切成
  空字串、整筆沒發音。
  切逗號會弄壞約 10 筆真的是「一個字被誤植空白切開」的（hypoploid
  `haipə, plɔid`、underbright、rotameter、pseudocoele 這類複合詞），
  換來 2,613 筆修好 —— 260:1，逐筆看過才決定的。
  **實機驗過**（2026-08-25）。注意這一項要重建 EC.DAT 並重新複製 SD 卡 ——
  音標轉換是轉檔期做的，光燒韌體不會生效。
  分隔符這一區前後踩了三次、每次都是靜默失敗，所以 `test_roundtrip.py`
  的 `test_phonemes()` 把五種樣態都留成回歸測試。
  **實機驗過**（2026-08-25，minute／lead／chance 等常用雙讀音字）
- **`r5` 不是音節**，是兒化韻
- 字面的 `\n` 兩個字元，不還原就會印在螢幕上

同一類問題也出現在字型：`ASCII_FIRST` 定義是 `0x20`，正則只認十進位就會
抓到 `0`，整張 ASCII 表偏移 32 格（查 `A` 拿到 `a`）。

### 四、使用者的口語描述比自動檢查值錢

「只有 01 有出來」直接指出是擦音問題（01 全是 `ma`）。「`zi` `zhi` 只有
啾啾聲，其餘正常」直接把範圍縮到噪音路徑。這兩句省下的排查時間，
比所有自動檢查加起來還多。

---

## 判斷時沒讀到的資料

| repo | 為什麼需要 |
|---|---|
| `pico_keyboard_ime_terminal` | 注音 IME 引擎的正本。**已 clone**，碼表由 `tools/gen_ime_tables.py` 解析取用。上游沒有 LICENSE 檔、碼表出處未寫明 —— 散布前要補 |
| `rp2040-retro-loader/HANDOVER.md` | 已 clone 但沒讀過。整合前應該讀 |
| `pico_keyboard` | 鍵盤方案的前身，可能有佈線細節 |

---

## 順手改了別的 repo

`rp2040-ili9341-infones` 的 README：把 Cubic 11 的授權從含糊的
「著作權屬於原作者」改成明確的 **SIL OFL 1.1**（上游 `ACh-K/Cubic-11`，
衍生自 M⁺ gothic 12r），並補上「`font_cjk.h` 是衍生資料，散布時應附上
OFL.txt」。**已 commit，未 push。**

---

## 文件地圖

| | |
|---|---|
| `docs/FORMAT.md` | 字典檔格式。§3.2 的扇區二分搜尋有個容易寫錯的邊界；§8 是常用詞索引 |
| `docs/U3-REPORT.md` | 合成器實驗。§4.5–4.7 記錄三輪聽判各抓到的 bug |
| `docs/PLAN.md` | 原始規劃。§5 已依實作結果改寫，§6 的風險表多數已關閉 |
| `tools/README.md` | 轉檔工具與合成器（Python） |
| `firmware/README.md` | C 實作（後台／合成器／UI／鍵盤／狀態機）、比對方法、移植踩過的坑 |
| `RetroDict/RetroDict.ino` | 板子上唯一碰硬體的檔案：ILI9341、矩陣掃描、SD |
| `loader_offset/*.py` | 偏移編譯的 linker script 生成器與 build 期佈局檢查（自 KeyboardTester 搬來，**不要手改產出的 .ld**）|
